{% extends 'base.html.twig' %}

{% block title %}Contacts{% endblock %}

{% block body %}
    <h1>Contacts</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Nom </th>
                <td>{{ contact.lastnameContact }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ contact.firstnameContact }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact.messageContact }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact.emailContact }}</td>
            </tr>
        </tbody>
    </table>
<div class="btn_back">
    <a class="btn btn-lg btn-primary btn_co prod" href="{{ path('app_contacts_index') }}">Retour</a>

    <a class="btn btn-lg btn-primary btn_co prod" href="{{ path('app_contacts_edit', {'id': contact.id}) }}">Modifier</a>

    {{ include('contacts/_delete_form.html.twig') }}
    </div>
{% endblock %}
