{% extends 'baseadmin.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
<div class="product_back">
    <h1>Categories</h1>

<h3>{{ category.nameCategory }}</h3>
   

    <a class="btn btn-lg btn-primary btn_co" href="{{ path('app_categories_index') }}">Revenir à la liste</a>

    <a class="btn btn-lg btn-primary btn_co" href="{{ path('app_categories_edit', {'id': category.id}) }}">Modifier</a>

    {{ include('categories/_delete_form.html.twig') }}
    </div>
{% endblock %}
