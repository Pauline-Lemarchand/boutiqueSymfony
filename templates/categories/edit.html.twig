{% extends 'baseadmin.html.twig' %}

{% block title %}Edit Categories{% endblock %}

{% block body %}
<div class="product_back">
    <h1>Modifier catégorie</h1>

    {{ include('categories/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

    <a class="btn btn-lg btn-primary btn_co" href="{{ path('app_categories_index') }}">Revenir à la liste</a>

    {{ include('categories/_delete_form.html.twig') }}
    </div>
{% endblock %}
